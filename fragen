wie viel arbeitsspeicher kann ich maximal nutzen in bezug auf 2^?    ?
jupiterliste erstellen?
wenn bitmap vom typ uint64_t
kann ich nicht eigentlich nur 2^32 * 2^6 bitmap elemente versenden
search keys sind root?
wie müsste das aussehen wenn ich mehr als 2^32 bzw. je nach proc anzahl elemente verschicke?
Frage wegen letzter Möglichkeit Parent Array zu erstellen
wie kommt man in die graph500 liste
auch mit eigener implementierung?
von wem wird diese überprüft?
wieso funktionieren 16 tasks nicht, ist was mit jupiter, danach haben 8 tasks auch nicht funktioniert?
validation für jeden search key?

the total number of vertices, 2SCALE. An implementation may use any set of N distinct integers to number the vertices, but at least 48 bits?? must be allocated per vertex number. Other parameters may be assumed to fit within the natural word of the machine. N is derived from the problem's scaling parameter.
